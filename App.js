/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow
 */

import React, {Fragment} from 'react';
import {  
  StyleSheet,
  Text
} from 'react-native';

const App = () => {
  return (
    <Fragment style={styles.body}>
      <Text>Target something now</Text>
      </Fragment>
  );
};

const styles = StyleSheet.create({
  
});

export default App;
